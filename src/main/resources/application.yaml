spring:
  application:
    name: token

  datasource:
    url: "jdbc:postgresql://localhost:5432/token"
    username: "token"
    password: "token"
  jpa:
    hibernate:
      ddl-auto: update
#      berhati hati menggunakan ddl-auto
  output:
    ansi:
      enabled: always

